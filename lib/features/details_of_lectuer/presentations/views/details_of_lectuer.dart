import 'package:flutter/material.dart';

class LectuerDetails extends StatelessWidget {
  const LectuerDetails({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
